只需要設定一次

當你執行這個：

git config --global user.name "你的名字"
git config --global user.email "你的信箱"

你其實是在修改：

👉 你電腦裡的一個 Git 設定檔

📁 它存在哪裡？

在 Windows 裡會存在：

C:\Users\你的帳號\.gitconfig



git add .
git commit -m "message"
git push